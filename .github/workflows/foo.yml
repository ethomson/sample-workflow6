name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: |
        echo '::set-output name=one::1'
        echo '::set-output name=two::2::zwei'
        echo '::set-output name=cf_output_BucketArn::arn:aws:s3:::kounta-lightspeed-upload-for-data-warehouse'
      id: foo
    - run: |
        echo ${{steps.foo.outputs.one}}
        echo ${{steps.foo.outputs.two}}
        echo ${{steps.foo.outputs.cf_output_BucketArn}}
    - run: |
        echo ${{toJSON(steps.foo.outputs)}}
