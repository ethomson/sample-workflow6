name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - run: |
        echo '::set-output name=one::1'
        echo '::set-output name=two::2::zwei'
      name: foo
    - run: |
        echo ${{toJSON(steps.foo)}}
